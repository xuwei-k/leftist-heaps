import com.qtamaki.lh.Heap
import com.qtamaki.lh.Empty

object Main {

  def main(args: Array[String]): Unit = {
//    val list: List[Ordered[Int]] = List(1, 2, 3)
//    val h:Heap[Ordered[Int]] = Heap.merge(Empty(), Empty())
//    val h2 = Heap.insert(1:Ordered[Int], h)
//    println("hello")
  }

}